---
description: Promote project instincts to global scope
agent: Everything OpenAI Codex:build
---

# Promote Command

Promote instincts in continuous-learning-v2: $ARGUMENTS

## Your Task

Run:

```bash
python3 "${CODEX_PLUGIN_ROOT}/skills/continuous-learning-v2/scripts/instinct-cli.py" promote $ARGUMENTS
```

If `CODEX_PLUGIN_ROOT` is unavailable, use:

```bash
python3 ~/.codex/skills/continuous-learning-v2/scripts/instinct-cli.py promote $ARGUMENTS
```

